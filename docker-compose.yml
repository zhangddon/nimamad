services:
  autopilot-agent:
    build: .
    container_name: autopilot-agent
    ports:
      - "8787:8787"
    volumes:
      - ./outputs:/app/outputs
      - ./logs:/app/logs
      - ./data:/app/data
    restart: unless-stopped
